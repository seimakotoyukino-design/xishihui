# 惜食惠（静态页面 Demo）

## 快速打开主页

在项目目录执行：

```bash
./run.sh
```

如果 8000 端口被占用，可改端口：

```bash
./run.sh 8080
```

然后在浏览器打开：

- `http://127.0.0.1:8000/index.html`（默认）
- `http://127.0.0.1:8080/index.html`（自定义端口示例）

## 文件说明

- `index.html`：页面结构
- `styles.css`：样式
- `app.js`：交互逻辑
- `run.sh`：本地一键启动脚本
